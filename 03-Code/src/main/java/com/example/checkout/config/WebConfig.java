package com.example.checkout.config;

import org.springframework.context.annotation.Configuration;

/**
 * Reserved for future cross-cutting web concerns (CORS, interceptors,
 * argument resolvers). Empty for v1 — no special needs.
 */
@Configuration
public class WebConfig {
}
